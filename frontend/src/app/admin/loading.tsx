/** Admin shell skeleton — covers cold compile of any /admin/* route. */
import PageSkeleton from '@/components/ui/PageSkeleton/PageSkeleton';

export default function AdminLoading() {
  return <PageSkeleton variant="admin" />;
}
